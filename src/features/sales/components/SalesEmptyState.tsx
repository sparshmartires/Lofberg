"use client"

import { EmptyState } from "@/components/ui/empty-state"

export function SalesEmptyState() {
  return <EmptyState description="No sales representatives found. Add a new sales representative to get started." />
}
